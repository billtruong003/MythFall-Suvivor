# 🛏️ HANDOFF — 2026-04-28

> Snapshot at end of session so future-you (or the next agent) can resume cold.
> Focus is "what to do next time you sit down", not full history.

---

## ✅ WHERE WE ARE

| Sprint | Status | Notes |
|---|---|---|
| **S0 — Setup + Bootstrap + Localization** | 🟢 Done | Bill ready (14 services, ~350ms), VN/EN localization loaded, BillStartup splash → MenuScene transition verified. APK build deferred. |
| **S1 Day 1 — Data + Player Components** | 🟢 Done | Stats, RuntimeStats, CharacterDataSO, PlayerHealth, TargetSelector, PlayerFacing, PlayerBase, MobileInputManager, events, state stubs. Kai_Data + Lyra_Data SO assets generated by Sprint1Setup. |
| **S1 Day 2 — Combat + Enemy + Spawner (code-side)** | 🟢 Done | MeleeCombat (animation-event driven + timer fallback), RangedCombat, MeleePlayer/RangedPlayer, Projectile, EnemyBase + SwarmerEnemy, WaveSpawner. Sprint2Setup editor menu builds 4 placeholder prefabs + AnimatorControllers + Layers + Materials + Swarmer_Data. |
| **S1 Day 2 — In-Editor Verification** | 🟡 NOT STARTED — **DO THIS FIRST** | Run Sprint2Setup, verify prefabs, drop into GameplayScene, confirm combat loop. |
| **S1 Day 3 — UI Panels + State Flow + InventoryService + VirtualJoystick** | 🟡 Not Started | Pending Day 2 verification. |
| S2 / S3 / S4 | 🟡 Not Started | — |

---

## 🎯 WHEN YOU SIT DOWN AGAIN — DO THESE IN ORDER

### Step 1 — Reopen Unity, expect 0 compile errors
- Latest fix: `DestroyImmediate` calls in [Sprint2Setup.cs](Assets/Mythfall/Scripts/Editor/Sprint2Setup.cs) qualified to `Object.DestroyImmediate(...)`.
- Two existing `BillSetupWizard.cs` warnings about obsolete `PlayerSettings.GetScriptingDefineSymbolsForGroup` — framework code, ignore.
- If new errors appear, screenshot to next session.

### Step 2 — Run the placeholder-prefab generator
```
Tools → Mythfall → Sprint 2 — Build Placeholder Prefabs
```
Should write to console:
```
[Sprint2Setup] DONE. 4 prefabs + 2 AnimatorControllers + 4 materials + Swarmer_Data + Layer 'Enemy'/'Projectile' configured.
```
Verify in Project window:
- `Assets/Mythfall/Prefabs/Players/{Kai,Lyra}.prefab`
- `Assets/Mythfall/Resources/Prefabs/Enemies/Swarmer.prefab`
- `Assets/Mythfall/Resources/Prefabs/Projectiles/Arrow.prefab`
- `Assets/Mythfall/Animations/{Player,Enemy}Animator.controller`
- `Assets/Mythfall/Materials/*.mat` (4 materials)
- `Assets/Mythfall/Resources/Enemies/Swarmer_Data.asset`

### Step 3 — Smoke-test the combat loop
1. Open `Assets/Mythfall/Scenes/GameplayScene.unity`.
2. Drag `Kai.prefab` into the scene at world origin.
3. Drag 3-5 `Swarmer.prefab` instances around the scene 3-5m from Kai.
4. Verify Kai has tag `Player` (the prefab sets it; verify Inspector shows `Tag: Player`).
5. Open `Assets/Mythfall/Scenes/BootstrapScene.unity`, press Play.
6. Bill bootstraps → Localization loads → `Register Pools` step fires → MenuScene loads. (We don't have a MainMenu Play button yet — manually load GameplayScene from Hierarchy or wait for Day 3 wiring.)
7. **Quick alternative for now:** open `GameplayScene` directly and press Play. Bill auto-detects scene 0 mismatch and bounces through Bootstrap, then back. Combat should run there without UI.
8. Expected: Swarmers chase Kai, Kai auto-attacks (timer fallback enables hitbox), Swarmers take damage and despawn after death anim window.

### Step 4 — Tell next session what worked / what didn't
Just paste the console output + describe what you saw on screen. Day 3 starts after Day 2 is verified.

---

## 🚧 DAY 3 PREVIEW (don't start yet)

After Day 2 verifies, Day 3 will deliver:
- 4 UGUI panel scripts (logic only — you build hierarchy in Editor): `MainMenuPanel`, `CharacterSelectPanel`, `HudPanel`, `GameOverPanel`, plus `SettingsOverlay` with language dropdown.
- `InventoryService` + `PlayerData` (saves selected character via `Bill.Save`).
- `VirtualJoystick` UGUI component → wires `MobileInputManager.MoveVector`.
- `GameBootstrap` registers states + UI panels + `CharacterSelectedEvent` flow.
- Each panel uses `LocalizedText` only — no hardcoded strings.

Each panel script will tell you exactly what GameObjects + components to create in the Editor (e.g., "Button named PlayButton with Image + Button + child TMP_Text named Label with LocalizedText component, key `ui.menu.play`").

---

## 🐛 KNOWN DEFERRED / IGNORED

| Issue | Plan |
|---|---|
| 3 `No Theme Style Sheet set to PanelSettings` warnings on Bill UI Toolkit panels | Defer to Sprint 1 Day 3 / Sprint 4 polish — assign default ThemeStyleSheet when building real UI |
| BillInspector duplicate menu item warning | Internal to BillInspector, ignore |
| 2 `BillSetupWizard.cs` obsolete-API warnings | Framework code — leave alone unless we modify those wizards |
| Audio listener warning in Bootstrap (orphan Camera GO) | Sprint0Setup re-run will clean up; not blocking |
| Animation events not firing on placeholder Animators | By design — `useTimerFallback = true` keeps combat testable. Flip false when real anim clips with events arrive. |
| `ModularTopDown.PlayerInputHandler` uses legacy `Input.GetAxisRaw` | Skipped — Mythfall uses `MobileInputManager` + (future) `VirtualJoystick`. |
| APK build / Android device test | Deferred per user request; resume any time. |

---

## 🏛️ ARCHITECTURE DECISIONS LOG

`Docs/ARCHITECTURE_DECISIONS.md` — read this first if anything looks weird.
- **CharacterLocomotion modified** with 5 additive lines for `ExternalRotationControl` pattern (search `MYTHFALL ADDITIVE` in the file).
- **`BillBootstrapConfig.defaultGameScene = ""`** — `GameBootstrap` owns first scene transition.
- **Namespace correction:** docs reference `BillTheDev.GameCore`, actual is `BillGameCore`.

---

## 🔁 QUICK COMMANDS / RECIPES

### Re-run any Sprint setup tool (idempotent)
```
Tools → Mythfall → Sprint 0 — Run Setup       # bootstrap + scenes + Android settings
Tools → Mythfall → Sprint 0 — Verify Setup    # 6/6 health check
Tools → Mythfall → Sprint 1 — Create Character Data   # Kai_Data + Lyra_Data SOs
Tools → Mythfall → Sprint 2 — Build Placeholder Prefabs   # 4 prefabs + controllers + materials
```

### Test localization at runtime
Drop `LocalizationTester.cs` ([Assets/Mythfall/Scripts/UI/LocalizationTester.cs](Assets/Mythfall/Scripts/UI/LocalizationTester.cs)) onto any GameObject in MenuScene, press Play. Console dumps every key in vi → en → vi.

### Find architectural breadcrumbs
```
grep -rn "MYTHFALL ADDITIVE" Assets/Mythfall/Scripts/LocomotionModular/
```
Shows every place we modified the locomotion framework.

---

## 📂 KEY PATHS CHEAT SHEET

```
Assets/Mythfall/Scripts/
  Core/Bootstrap/GameBootstrap.cs          # AddStep entry point
  Core/Events/{GameEvents, LanguageChangedEvent}.cs
  Core/States/MythfallStates.cs            # Day 3 fills these in
  Core/Localization/LocalizationService.cs # IService + IInitializable
  Characters/{CharacterStats, RuntimeCharacterStats, CharacterDataSO}.cs
  Player/{PlayerBase, PlayerHealth, PlayerFacing, TargetSelector,
          PlayerCombatBase, MeleeCombat, RangedCombat,
          MeleePlayer, RangedPlayer, HitboxRelay}.cs
  Enemy/{EnemyDataSO, EnemyBase, SwarmerEnemy}.cs
  Gameplay/{Projectile, WaveSpawner}.cs
  Skills/SkillCore.cs                      # Stub — Sprint 3 fills
  Input/MobileInputManager.cs
  UI/{LocalizedText, LocalizationTester}.cs
  Editor/{Sprint0Setup, Sprint1Setup, Sprint2Setup}.cs
  LocomotionModular/Core/CharacterLocomotion.cs   # See MYTHFALL ADDITIVE comments

Assets/Mythfall/Resources/
  Localization/lang_{vi,en}.json
  Characters/{Kai,Lyra}_Data.asset
  Enemies/Swarmer_Data.asset
  Prefabs/Enemies/Swarmer.prefab
  Prefabs/Projectiles/Arrow.prefab
  BillBootstrapConfig.asset

Assets/Mythfall/Prefabs/Players/{Kai,Lyra}.prefab
Assets/Mythfall/Animations/{Player,Enemy}Animator.controller
Assets/Mythfall/Materials/{Kai,Lyra,Swarmer,Arrow}_Mat.mat
Assets/Mythfall/Scenes/{Bootstrap,Menu,Gameplay}Scene.unity
```

---

## 🛌 GOOD NIGHT

Step 2 is the only blocking action when you come back. Everything else is reading + testing.

If anything compiles but behaves weird → check `Docs/ARCHITECTURE_DECISIONS.md` first, then tell next session what you observed (with console output if possible).
